// @ts-check
// import express from 'express';

// const app = express();
// app.use(express.urlencoded({ extended: false }));
// app.use(express.json());


// app.get('/test', (_, res, next) => {
//   res.type('text/html');
//   res.send('answer');
//   next();
// });

// app.post('/test', (_, res, next) => {
//   res.type('text/html');
//   res.send('answer');
//   next();
// });

// app.on('error', err => {
//   console.error('server error', err)
// });

// await new Promise((resolve) => 
//   app.listen(+process.env.port, process.env.host, () => resolve()));

// export { app }
